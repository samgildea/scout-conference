import styled from "@emotion/styled"


const CountdownContainer = styled.div`
  display: flex;
`

const CountdownTime = styled.div`
  padding-right: 2rem;
  padding-left: 2rem;
`

export default {CountdownContainer, CountdownTime}